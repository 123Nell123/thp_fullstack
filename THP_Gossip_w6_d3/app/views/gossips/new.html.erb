<h1>Gossips#new</h1>
<p>Find me in app/views/gossips/new.html.erb</p>

<div class style="background-color:powderblue;">
<% if @gossip.errors.any? %>
  <p>⚠️ Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p><ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
</div>



formulaire_tag
<%= form_tag url_for(action: 'create'), method: "post" do %>

      <%= label_tag 'Title' %>
      <%= text_field_tag 'title', @gossip.title %>
      <br /> <br />

      <%= label_tag 'Body' %>
      <%= text_area_tag 'content', @gossip.content %>
      <br /> <br />

      <%= label_tag 'Author' %>
      <%= text_field_tag 'author', @gossip.user_id %>
      <%= hidden_field_tag 'author', "babar" %>
      <br /> <br />

      <%= submit_tag "Create Post" %>
 <% end %>